import type { BlockNoteRenderProps } from '../types';

export function renderMenubarBlock(_props: BlockNoteRenderProps) {
  return (
    <div className="py-2 px-3 border border-dashed border-line text-mute text-sm font-joystix my-1">
      Menubar
    </div>
  );
}
