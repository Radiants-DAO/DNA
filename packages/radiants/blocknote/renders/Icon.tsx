import { Icon } from '../../icons/runtime';
import type { BlockNoteRenderProps } from '../types';

export function renderIconBlock(_props: BlockNoteRenderProps) {
  return <Icon name="plus" large />;
}
