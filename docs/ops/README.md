# Ops Docs

Current operational docs live here.

- `qc-visual-t1-*.md` tracks the active manual visual QA workflow.
- `paper/` holds the current Paper recreation prompt pack, checklists, and brand brief.
- `playground-fix-log.md` is the active append-only fix log used by the playground workflow.

Historical trackers and dated session logs live in `../../archive/ops/`.
