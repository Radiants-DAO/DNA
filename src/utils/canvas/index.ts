/**
 * Canvas utilities for design mode interaction
 *
 * Ported from Webstudio's canvas system with adaptations for RadFlow.
 */

export {
  createInterceptor,
  applyPointerEventsStyle,
} from "./interceptor";

export type {
  Interceptor,
  InterceptorMode,
} from "./interceptor";
