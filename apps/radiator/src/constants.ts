export const defaultRpcEndpoint = 'https://api.devnet.solana.com';

/** Flip to false to enable real SDK calls (only createConfig is live initially) */
export const USE_MOCK_CHAIN = true;

/** Flip to false to use real DAS data from on-chain */
export const USE_MOCK_DATA = true;
