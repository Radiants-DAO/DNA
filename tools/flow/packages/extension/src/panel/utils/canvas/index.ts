/**
 * Canvas utilities for design mode interaction
 *
 * Ported from Flow 0 canvas system.
 */

export {
  createInterceptor,
  applyPointerEventsStyle,
} from "./interceptor";

export type {
  Interceptor,
  InterceptorMode,
} from "./interceptor";
