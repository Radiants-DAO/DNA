[package]
name = "notify-poc"
version = "0.1.0"
edition = "2024"

[dependencies]
notify = "7"
notify-debouncer-mini = "0.5"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time"] }

[dev-dependencies]
tempfile = "3"
