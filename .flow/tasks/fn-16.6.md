# fn-16.6 Document DNA integration scope

## Description
Document the DNA (Design Nexus Architecture) integration scope and monorepo structure for Phase 2.

## DNA Monorepo Structure

```
~/Desktop/dev/dna/
├── packages/
│   ├── core/                # Base component library (future)
│   └── rad-os/              # ← theme-rad-os moves here
│       ├── tokens/
│       ├── components/
│       ├── assets/
│       ├── ai-skills/
│       └── package.json
├── tools/
│   └── dna-cli/             # Future CLI specs
├── docs/
│   └── theme-spec.md        # Unified spec (merged from 3 sources)
├── pnpm-workspace.yaml
└── README.md
```

## Integration Points

1. **theme-rad-os migration**
   - Source: `/Users/rivermassey/Desktop/dev/radflow-tauri/packages/theme-rad-os/`
   - Destination: `~/Desktop/dev/dna/packages/rad-os/`
   - Contains: 30+ components, tokens.css, dark.css, typography.css, AI skills

2. **Theme spec merge**
   - Input 1: `docs/theme-spec.md`
   - Input 2: `docs/design-system-infrastructure.md`
   - Input 3: `~/Downloads/dna-theme-spec.md`
   - Output: `~/Desktop/dev/dna/docs/theme-spec.md`

3. **ai-skills/ usage**
   - RadFlow reads ai-skills/ for prompt building
   - External tools (Claude Code, Cursor) can also use them
   - Located in theme packages, not centralized

## What Stays in radflow-tauri

- `packages/bridge/` - RadFlow's injection mechanism
- All src/ and src-tauri/ code
- .flow/ (after reset in Phase 3)

## Update Progress Doc

Add DNA integration scope documentation to progress doc under "Epic 1: Discovery & Audit > Documented dna integration scope".
## Acceptance
- [ ] DNA monorepo structure documented
- [ ] theme-rad-os migration scope confirmed (source → destination)
- [ ] Theme spec merge inputs/output documented
- [ ] ai-skills/ usage pattern documented (RadFlow + external tools)
- [ ] What stays in radflow-tauri listed (bridge/, src/, src-tauri/)
- [ ] DNA integration section added to progress doc
## Done summary
Documented DNA integration scope for Phase 2:
- Minimal scaffold: dna/docs/theme-spec.md + README.md (not full monorepo)
- Theme spec merge: 3 sources → 1 unified spec with manual conflict resolution
- ai-skills/ dual usage: RadFlow reads + external tools (Claude Code, Cursor) read
- What stays in radflow: bridge/, src/, src-tauri/, .flow/
- theme-rad-os migration is future scope, not Phase 2
Added DNA integration section to progress doc with structure diagrams.
## Evidence
- Commits:
- Tests:
- PRs: